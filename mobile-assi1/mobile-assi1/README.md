# Technical Reflection

The objective of this assignment is to apply the Cross-Platform Shell Architecture by creating a native app that acts as a container for web content. Using  React Native and WebView, the app loads Google Search inside a SafeAreaView, demonstrating how a native shell wraps a web interface. 